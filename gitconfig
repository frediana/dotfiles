[user]
	name = Adrien Frediani
	email = frediani.adrien@gmail.com
[alias]
	st=status
	br=branch
	co=checkout
	ri=rebase -i
	cp=cherry-pick
	df = diff --color --color-words --abbrev
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    tempo=commit -a -m tempo
    pop=reset HEAD~
    amend=commit --amend
[color]
	ui = auto
	interactive = auto
[diff]
	algorithm = histogram
	renames = copy
	tool = Kaleidoscope
	wordRegex = [^[:space:]]
	mnemonicprefix = true
[color "branch"]
	current = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red reverse
  	new = green reverse
  	whitespace = white reverse
[color "status"]
  	added = green
  	changed = red
  	untracked = yellow
  	branch = magenta
[github]
	user = frediana
[credential]
	helper = osxkeychain
